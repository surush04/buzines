{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install": {
      "commands": ["npm ci --prefix frontend"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": [
        "cd frontend && node scripts/patch-env.mjs",
        "npm run build --prefix frontend"
      ]
    }
  },
  "deploy": {
    "startCommand": "npx serve -s frontend/dist/frontend/browser -l ${PORT:-8080}"
  }
}
